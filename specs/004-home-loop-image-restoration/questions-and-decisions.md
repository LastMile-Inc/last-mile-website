# Questions and decisions

| Question | Decision |
| --- | --- |
| Which image is the exact original? | Repository history shows the immediately preceding Home implementation used `accountable-operations-loop-v3.png`. |
| Should the three-column layout be reverted? | No. Keep it and preserve all current definition wording and spacing. |
| How should the figure be centered? | Wrap it in `.lm-premium-loop-center` and use Flexbox `align-items: center` and `justify-content: center`. |
| Can padding be used to adjust the visual position? | No. The center container stretches with the row and centers the figure structurally. |
| Should v4 remain as an unused asset? | No. The founder explicitly directed that it be discarded entirely. It remains recoverable from Git history. |

