# Implementation plan

1. Create the spec-kit documents for the architect reference portal.
2. Add a new lazy-loaded public route at `/resources/architect-reference-center`.
3. Build the page with existing Last Mile page patterns and approved color tokens.
4. Implement the protocol compatibility calculator with in-browser scoring only.
5. Implement the telemetry bandwidth estimator with Recharts visualization and branded controls.
6. Implement code-native zero-trust diagram views with explicit authority boundaries.
7. Add downloadable CSV matrices for SOC 2, ISO/IEC 27001, and IEC 62443 reference mappings.
8. Wire the route into Resources navigation surfaces and the Resources overview page.
9. Run repository validation and stop after the local site build is clean.

